<%= content_tag(:div, '', id:'visits', data: {
    controller: "react",
    visit: @visit.as_json(include: { 
                                    owner: { only: [ :last_name, :id, :first_name ] } , 
                                    pet: { only: :name }, 
                                    animal: {only: :name } 
                                    })
})%>

<%= render partial: "partials/structure2", locals: {model_name: "visit", 
                                                         object: @visit,
                                                         primary: "visit_data", 
                                                         secondary: nil,
                                                         sidebar: "visit_sidebar"} %>